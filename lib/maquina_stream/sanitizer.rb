# frozen_string_literal: true

module MaquinaStream
  # Placeholder. Phase 2 (render pipeline) implements this.
  # Contract: allowlist plus URL hardening, the last pass before output.
  class Sanitizer
  end
end
