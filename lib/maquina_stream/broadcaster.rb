# frozen_string_literal: true

module MaquinaStream
  # Placeholder. Phase 2 (render pipeline) implements this.
  # Contract: frame coalescing and Turbo Stream emission, through the
  # transport seam (MaquinaStream.config.transport).
  class Broadcaster
  end
end
