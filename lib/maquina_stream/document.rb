# frozen_string_literal: true

module MaquinaStream
  # Placeholder. Phase 2 (render pipeline) implements this.
  # Contract: .new(markdown, config:) => #blocks, #sealed_blocks, #open_block
  class Document
  end
end
