# frozen_string_literal: true

module MaquinaStream
  # Placeholder. Phase 2 (render pipeline) implements this.
  # Contract: #id #index #markdown #html #digest #sealed? #line_range
  # Block ids are index-derived, never content-derived.
  class Block
  end
end
