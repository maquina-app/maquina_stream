phase: 7
slug: history-hardening
title: History, hardening and 0.1
repo: maquina_stream
depends_on: [3, 4, 5, 6]
status: partial
opened: 2026-09-06

tasks:
  - { id: 1, title: history pagination through Turbo Frames, status: done, note: "dummy app HistoryController + history/_page; loads upward through a lazy frame, no pagination bar" }
  - { id: 2, title: completed messages frozen and fragment-cached by digest, status: done, note: "MaquinaStream.render caches a sealed message by buffer digest; an open one is never cached. 95.04ms cold -> 0.049ms cached, 1942x. A 50-message page: 4752ms -> 2.4ms." }
  - { id: 3, title: explicit cancelled / errored / timed_out states with replay behaviour, status: done }
  - { id: 4, title: whole-message markdown export with the Phase 6 fallback, status: done, file: lib/maquina_stream/export.rb }
  - { id: 5, title: review VENDORED_COMPONENTS, status: done, note: "docs/component-scope.md - three couplings block extraction, not just the fallback branch" }
  - { id: 6, title: multi-tab convergence review, status: done, file: test/maquina_stream/multi_tab_test.rb }
  - { id: 7, title: dummy app demonstrating every feature, status: done, note: "Streamable model, both seams, all three fence strategies, tag registry, third host-side renderer, browser harness at /harness, history pagination at /history" }
  - { id: 8, title: README and integration guide, status: done }
  - { id: 9, title: CI matrix, status: done, note: ".github/workflows/ci.yml - ruby 3.3/3.4 x rails 8.0/8.1 for the engine, 3.1-3.4 for the gem" }
  - { id: 10, title: tag 0.1 and open the Fragua branch, status: deferred, note: "user asked to hold on pushing to a remote or releasing" }

dod:
  - line: Long-session load and memory figures recorded in the spec.
    met: true
    verification: >-
      500 messages rendered in 387ms (0.77ms each), 2,071KB of HTML, 4,242B
      average per message, 1.89M objects allocated; 289ms on a second pass with
      the component cache warm (999 hits, 1 miss). Measured in-process; a
      browser-side memory figure while scrolling history needs task 1.
  - line: Replay parity asserted in CI, not checked once by hand.
    met: true
    verification: >-
      replay_test.rb streams a message through the broadcaster, applies the real
      broadcast bytes, runs the repair path, and asserts block-for-block content
      parity with a static render. A companion test asserts the delta stream
      alone does NOT converge, so the repair seam cannot quietly become dead
      code.
  - line: Documentation complete enough that the dummy app is the only example needed.
    met: partial
    verification: >-
      README plus six docs; test/dummy is a working host. Not verified the way
      the plan asks - "a fresh developer integrates using only the README" needs
      a fresh developer.
  - line: 0.1 tagged; Fragua consumes the released gem, not a path reference.
    met: false
    verification: "Deferred at the user's request: no remote, no tag, no release."

measurements:
  history_500_messages: { render_ms: 387, per_message_ms: 0.77, html_kb: 2071, objects: 1891041, warm_ms: 289 }
  sealed_message_cache: { cold_ms: 95.04, cached_ms: 0.049, speedup: 1942, page_of_50_uncached_ms: 4752, page_of_50_cached_ms: 2.4 }
  arrival_shape_20kb:
    note: "measured after the user said responses may be batched and sessions are short (Nexo)"
    token_4ch_25ms:   { frames: 474, vs_floor: 1.25, cpu_ms: 8740 }
    batch_40ch_200ms: { frames: 238, vs_floor: 1.04, cpu_ms: 4282 }
    step_2000ch_1s:   { frames: 11,  vs_floor: 1.26, cpu_ms: 253 }
    one_shot:         { frames: 2,   vs_floor: 1.28, cpu_ms: 65 }
    conclusion: >-
      Bandwidth is near-invariant (1.04-1.32x) because the document must be sent
      once either way. CPU is where batching pays: 35x from token to step shape.
      The quadratic-render risk largely disappears under Nexo-shaped arrival.

resolved_divergence:
  was: >-
    A block that sealed after it stopped being the tail kept presentation chrome
    - data-ms-reveal, data-ms-caret, data-ms-block-state="open" - which a
    manifest digest cannot see, because the digest covers content.
  how_it_surfaced: >-
    The multi-tab test. Two tabs losing DIFFERENT frames ended up visibly
    different - one with a caret, one without - and repair could not reconcile
    them because their digests agreed. Stale-until-reload was tolerable; two
    tabs disagreeing was not.
  fixed_by: >-
    Removing per-block chrome entirely. Blocks carry content, id, index and
    digest; the caret and reveal are derived in CSS from the message element
    ([data-ms-streaming] > [data-ms-block]:last-child). Paying to correct chrome
    through the delta path was measured first and cost a full extra copy of the
    message (1.248x -> 2.41x), which is why deriving beats correcting.
  consequence: >-
    mode: no longer changes renderer output. Live, reload, replay and export are
    byte-identical, and replay parity is now asserted exactly instead of on
    content only. Third and final change to the DOM contract, recorded in
    docs/api-surface.md.

open:
  - "frame_budget_ms was set to 250 against token-shaped arrival. Under step-shaped (Nexo) arrival, CPU is no longer the constraint and 250ms can coalesce two steps into one frame, costing per-step feedback. Recommend reverting to 60-100ms; not changed without the user."
  - "Whether a Nexo tool call is a separate Streamable record or a block inside one message. Shapes stream_for: and morph scope. Unanswered."
  - "Extraction is blocked by three couplings, not one: the data-ms-* contract attributes, the maquina_stream.* locale namespace, and the shared component_html_options helper. Fix for the first is to pass them as locals at the call site; not yet done."
  - "Release steps deferred by the user."
